# maps/
# Costmap data is NOT required for standalone tracking (MVP uses /odom + /tf
# only).  This directory is reserved for the U10 collision gate and the U11
# explorer integration (costmap/collision-monitor consumption).  Until a
# costmap interface is explicitly wired, collision projection is NOT claimed
# implemented (docs/ros2_interface_contract.md / KTD12).
